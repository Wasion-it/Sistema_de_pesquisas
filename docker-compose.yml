version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sistema_de_pesquisas
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d sistema_de_pesquisas"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - pesquisas_internal
    deploy:
      replicas: 1
      restart_policy:
        condition: any
      labels:
        - "portainer.stack=sistema-de-pesquisas"
        - "portainer.service=postgres"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  backend:
    image: sistema-de-pesquisas-backend:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: backend
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql+psycopg2://postgres:postgres@postgres:5432/sistema_de_pesquisas
      DEBUG: "false"
      JWT_SECRET_KEY: change-this-secret-for-production
      CORS_ALLOW_ORIGINS: https://systemrh.wasion.com.br
    command: >
      sh -c "python -m alembic upgrade head &&
      python -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8000/api/v1/health')"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 40s
    networks:
      - pesquisas_internal
      - controle_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: any
      labels:
        - "portainer.stack=sistema-de-pesquisas"
        - "portainer.service=backend"

        - "traefik.enable=true"
        - "traefik.docker.network=controle_overlay"
        - "traefik.http.routers.pesquisas-api.rule=Host(`systemrh.wasion.com.br`) && PathPrefix(`/api`)"
        - "traefik.http.routers.pesquisas-api.entrypoints=websecure"
        - "traefik.http.routers.pesquisas-api.tls=true"
        - "traefik.http.routers.pesquisas-api.priority=100"
        - "traefik.http.services.pesquisas-api.loadbalancer.server.port=8000"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  frontend:
    image: sistema-de-pesquisas-frontend:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: frontend
      args:
        VITE_API_URL: /api/v1
    networks:
      - controle_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: any
      labels:
        - "portainer.stack=sistema-de-pesquisas"
        - "portainer.service=frontend"

        - "traefik.enable=true"
        - "traefik.docker.network=controle_overlay"
        - "traefik.http.routers.pesquisas.rule=Host(`systemrh.wasion.com.br`)"
        - "traefik.http.routers.pesquisas.entrypoints=websecure"
        - "traefik.http.routers.pesquisas.tls=true"
        - "traefik.http.routers.pesquisas.priority=10"
        - "traefik.http.services.pesquisas.loadbalancer.server.port=80"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  postgres-data:

networks:
  controle_overlay:
    external: true

  pesquisas_internal:
    driver: overlay
    attachable: true